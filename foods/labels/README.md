# Label summaries

One file per labelled product, distilled from label screenshots:
`<brand>-<product>.md` (lowercase, hyphenated).

Each file records: brand, product name, per-100 g nutrition table, stated
serving size, ingredients highlights, and the date the label was read. The
matching row in `../dictionary.md` links here and uses these values verbatim.
